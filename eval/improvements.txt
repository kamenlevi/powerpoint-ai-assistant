To handle requests for deleting a slide by index, use the getSlideByIndex helper and check for null before calling delete.
```javascript
if (prompt.includes("delete slide")) {
  const index = parseInt(prompt.match(/delete slide (\d+)/)[1]);
  const slide = await getSlideByIndex(index - 1);
  if (slide) {
    slide.delete();
    await context.sync();
  } else {
    console.log(`Slide ${index} does not exist.`);
  }
}
```

To handle requests for adding information that requires a web search, suggest a workaround and provide an example of how to add the result as a text box.
```javascript
if (prompt.includes("add a slide with the definition of")) {
  console.log("To answer this question, please perform a web search, copy the result, and use the addTextBox helper to add it to the slide.");
  // Example: await addTextBox("Search result", { left: 100, top: 100, width: 300, height: 50 });
}
```

To handle requests for generating a presentation with a title and background, use the addSlide helper and set the background fill.
```javascript
if (prompt.includes("generate a")) {
  if (prompt.includes("1-slide presentation with a title and a solid color background")) {
    await addSlide("title", "New Presentation", "", "");
    const slide = await getSlideByIndex(0);
    slide.background.fill.setSolidColor("#ffffff");
    await context.sync();
  }
}
```

To handle requests for adding a hyperlink to specific text on a slide without overwriting existing text, find the shape with the matching text and set the hyperlink.
```javascript
if (prompt.includes("Add a hyperlink to the text")) {
  const text = prompt.match(/'([^']+)'/)[1];
  const url = prompt.match(/https?:\/\/([^']+)/)[1];
  const slideIndex = prompt.match(/on the (\w+) slide/)[1] === "first" ? 0 : parseInt(prompt.match(/on the (\d+) slide/)[1]) - 1;
  const slide = await getSlideByIndex(slideIndex);
  for (const shape of slide.shapes.items) {
    try {
      const textRange = shape.textFrame.textRange;
      if (textRange.text.includes(text)) {
        textRange.hyperlink.address = url;
        await context.sync();
      }
    } catch (e) {}
