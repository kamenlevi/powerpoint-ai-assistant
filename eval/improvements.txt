To handle requests for adding images to a specific slide, use the insertImage helper with the correct slide index and image details.
```javascript
if (prompt.includes("add a") && prompt.includes("image to slide")) {
  const slideIndex = parseInt(prompt.match(/slide (\d+)/)[1]) - 1;
  const imageType = prompt.match(/add a (\w+) image/)[1];
  const details = prompt.match(/(\w+) (\w+)/);
  await insertImage(slideIndex, imageType, [details[1], details[2]], { left: 100, top: 100, width: 200, height: 150 });
}
```

To handle requests for adding information that requires a web search, suggest a workaround and provide an example of how to add the result as a text box.
```javascript
if (prompt.includes("add a slide showing the")) {
  console.log("To answer this question, please perform a web search, copy the result, and use the addTextBox helper to add it to the slide.");
  // Example: await addTextBox("Search result", { left: 100, top: 100, width: 300, height: 50 });
}
```

To handle requests for creating charts, use the insertImage helper with a chart type.
```javascript
if (prompt.includes("add a") && prompt.includes("chart")) {
  const slideIndex = parseInt(prompt.match(/slide (\d+)/)[1]) - 1;
  const chartType = prompt.match(/add a (\w+) chart/)[1];
  await insertImage(slideIndex, "charts-diagrams", [chartType, "chart"], { left: 100, top: 100, width: 400, height: 300 });
}
```

To handle requests for deleting a slide by index, handle the case where the slide does not exist.
```javascript
if (prompt.includes("delete slide")) {
  const index = parseInt(prompt.match(/delete slide (\d+)/)[1]);
  try {
    const slide = await getSlideByIndex(index - 1);
    slide.delete();
    await context.sync();
  } catch (e) {
    console.log(`Slide ${index} does not exist.`);
  }
}
```

To handle requests for making text bold, target the correct shape and text range.
```javascript
if (prompt.includes("make the") && prompt.includes("bold")) {
  const slideIndex = prompt.match(/on the
