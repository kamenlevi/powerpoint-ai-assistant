To replace text across the deck, ensure all text elements are updated.
```javascript
// Replace all instances of 'Acme' with 'Globex'
presentation.slides.load("items/id, items/shapes");
await context.sync();
for (const slide of presentation.slides.items) {
  for (const shape of slide.shapes.items) {
    try {
      shape.textFrame.textRange.text = shape.textFrame.textRange.text.replace(/Acme/g, "Globex");
    } catch (e)
